# Local dev helpers — never commit (may reference real accounts)
__preview-setup.html
__*.html

# Brainstorming mockups (scratch, regenerated per session)
.superpowers/

# Test runner — installed, never committed
node_modules/
coverage/
